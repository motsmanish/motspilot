# ─── Multi-Model Consensus API Keys ─────────────────────────────
# Used by the consensus service to fan out prompts to multiple LLMs.
# Get your keys from each provider's developer console.
# NEVER commit actual keys — this file is a template only.

ANTHROPIC_API_KEY=
OPENAI_API_KEY=
GEMINI_API_KEY=
