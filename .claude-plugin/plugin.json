{
  "name": "mots",
  "version": "1.1.0",
  "description": "6-phase AI pipeline for adding features to existing applications. Framework-agnostic. Consensus + Architecture + Development + Testing + Verification + Delivery.",
  "author": {
    "name": "Manish"
  },
  "repository": "https://github.com/motsmanish/motspilot",
  "license": "MIT",
  "keywords": [
    "pipeline", "architecture", "development",
    "testing", "verification", "delivery",
    "feature-development", "ai-pipeline"
  ],
  "userConfig": {
    "ANTHROPIC_API_KEY": {
      "description": "Anthropic API key for consensus phase (Claude model). Optional — consensus is skipped if missing.",
      "sensitive": true
    },
    "OPENAI_API_KEY": {
      "description": "OpenAI API key for consensus phase (GPT-4o model). Optional — consensus is skipped if missing.",
      "sensitive": true
    },
    "GEMINI_API_KEY": {
      "description": "Google API key for consensus phase (Gemini model). Optional — consensus is skipped if missing.",
      "sensitive": true
    }
  }
}
